# Go workspace file — local-dev only; ties this module to sibling checkouts
# (../Oblikovati, ../Oblikovati.AddIns). Not committed; CI checks out siblings.
go.work
go.work.sum

# Wiki publish staging (assembled pages + generated API Docs)
/build/

# Binaries / test artifacts
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.out

# Local git worktrees (superpowers:using-git-worktrees convention)
.worktrees/
